application:
    name: helloworld
    site-root: /helloworld
    mode: ReadOnly | ReadWrite
    session: Distributed | Locale

servers:
    database:
        master: mysql://user:pass@localhost/db?persist
        slave-1:

    cache:
        master: memcached://localhost?persist
        


